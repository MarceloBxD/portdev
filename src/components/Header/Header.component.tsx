import React from "react";
import { motion as m } from "framer-motion";
import styles from "./Header.module.scss";

export default function Header() {
  return <m.header className={styles.headerContainer}>Header</m.header>;
}
